/**
 * The colour field every glass surface frosts. Pure CSS; see .aurora in
 * globals.css. Rendered once in the root layout, behind everything.
 */
export default function Aurora() {
  return <div className="aurora" aria-hidden="true" />;
}
